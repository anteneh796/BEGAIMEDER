import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { events } from "@/lib/content";
export default function EventsPage(){return <><SiteHeader/><main><section className="content-section events-page"><span className="eyebrow">What's happening</span><h1 className="section-title">Events that bring our community together.</h1><div className="event-list">{events.map(event=><article key={event.id}><div className="event-date">{event.date}</div><div><h2>{event.title}</h2><p>{event.description}</p><small>{event.time} · {event.location}</small></div><a href="/contact">Ask about this →</a></article>)}</div></section></main><SiteFooter/></>}
